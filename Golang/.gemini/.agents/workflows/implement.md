---
description: 按 tasks.md 逐 Phase 执行代码实现，完成任务闭环并可断点续做。
---

识别当前项目，并执行代码实施流程。

调用 `speckit-implement` 完成当前阶段工作。
项目技能与语言技能由 Agent 在运行时识别并决定是否补充加载。
按任务完成实现并更新任务状态；若三件套不完整，先报告阻塞项，不直接实施。

用户输入：$ARGUMENTS
