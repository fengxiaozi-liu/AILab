# 修复任务模板

在 `Tasks` 阶段，按以下结构写入 `specs/<feature>/issue.md`。

```md
## 修复任务

- [ ] T001 [ISSUE-001] [修复任务描述] `[目标文件或产物]`
- [ ] T002 [ISSUE-001] [验证任务描述] `[验证方式或脚本]`
```

要求：

- 任务编号必须顺延且唯一
- 每个任务都必须显式标注所属 `ISSUE-xxx`
- 任务描述必须是实现导向或验证导向，不得写成模糊目标
- 任务状态只允许使用 `- [ ]` 与 `- [✅️]`
